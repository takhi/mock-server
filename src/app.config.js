const config = {
  JSON_STRUCTURE: {
    CONDITION: 'withReqBody',
    ARRAY: 'mockResponses',
    ID: 'reqBody',
    OUTPUT: 'response',
    CONFIG: 'resConfig',
    _DEFAULT: '_default'
  }
}

module.exports = config;